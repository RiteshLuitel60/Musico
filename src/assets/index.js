// Import the loader image
import loader from "./loader.svg";

// Import the logo image
import logo from "./logo.svg";

// Export both images so they can be used in other files
export { logo, loader };
